# GPT-5.6 Luna information assistance

Read this only when large or repetitive evidence materially reduces controller context or cost.

- Keep one project-scoped read-only Luna assistant scope using only an available authorized `gpt-5.6-luna` route; choose effort from the evidence without a fixed medium default; deduplicate source-bound material. If unavailable, omit this optional helper; do not substitute `gpt-6-luna`. An idle task may receive its next turn through explicit supported model and thinking fields. Only use a replacement when the API cannot carry that route or a context/ownership boundary requires one; never change a running turn's route.
- Luna may summarize reports, logs, and tests; extract progress, evidence, blockers, approvals, and terminal state; deduplicate status; and draft the update.
- A Luna result is advisory; verify primary evidence.
- Do not use Luna for a few lines, routine updates, or to appear busy.
- GPT-5.6 Luna cannot write or modify code, choose execution models or review lanes, make architecture decisions, review, accept, or mark work complete. This reference is exclusively for `gpt-5.6-luna`, not GPT-6 Luna execution.
